1.Grpc调用中平台类型出现兼容性问题[已解决]
    备注:更换数据类型,采用大多数语言都拥有的类型,避免无符号类型的使用
2.服务注册之后要10s左右后才会开始调度[无需处理]
    备注:由于定时任务30s刷新一次,服务注册时只会保存在切片上,刷新后才会放在任务管理器中
3.任务注册管理器的io多路复用没有监听[未解决(暂时用锁替代)][已修复]
    备注:是因为创建了任务注册管理器,主线程中的任务注册管理器和grpc的任务注册管理器不是同一个

已完成功能:
1.定时任务触发(全部完成)[后期将替换cron库,全部由系统自己实现任务扫描]
2.服务注册和心跳(全部完成)
3.服务调用和回调(全部完成)
4.超时任务扫描处理(全部完成)
5.sql 99语法调整(全部完成)
6.任务重试(部分完成)[后续版本优化为重试队列,预计在内存中进行]
7.部分用户对接接口(部分完成)[目前实现的并不完善,只是为了提供开发者进行调试]


未完成功能:
1.预警服务
2.用户对接接口api
3.用户可视化界面
5.报表任务
6.用户体系服务
7.路由体系

后续版本预开发功能:
1.重试队列(去除循环查库机制)
2.模式启动(单机启动模式和集群启动模式)
3.集群分片同步机制(目前服务仅支持单机部署,暂不支持集群,后续需要开发分片机制支持集群)
4.多种数据库支持计划(目前sql已经全部支持sql99语法,后期如果时间引发问题,不排除将时间改成时间毫秒值)

总体:已基本完成定时任务核心功能,接下里进行补充用户api以及定制化业务
